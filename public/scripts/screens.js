FlappyGame.screens['menu-screen'] = (function(play) {
	'use strict';
	
	function initialize() {}
	
	function run() {
		play.initialize('menu-screen');
	}
	
	return {
		initialize : initialize,
		run : run
	};
}(FlappyGame.play));

FlappyGame.screens['game-screen'] = (function(play) {
	'use strict';
	
	function initialize() {}
	
	function run() {
		play.initialize('game-screen');
	}
	
	return {
		initialize : initialize,
		run : run
	};
}(FlappyGame.play));

FlappyGame.screens['game-over-screen'] = (function(play) {
	'use strict';
	
	function initialize() {}
	
	function run() {
		play.initialize('game-over-screen');
	}
	
	return {
		initialize : initialize,
		run : run
	};
}(FlappyGame.play));



FlappyGame.screens['controls-screen'] = (function(play) {
	'use strict';
	
	function initialize() {}
	
	function run() {
		play.initialize('controls-screen');
	}
	
	return {
		initialize : initialize,
		run : run
	};
}(FlappyGame.play));

FlappyGame.screens['score-screen'] = (function(play) {
	'use strict';
	
	function initialize() {}
	
	function run() {
		play.initialize('score-screen');
	}
	
	return {
		initialize : initialize,
		run : run
	};
}(FlappyGame.play));


FlappyGame.screens['credits-screen'] = (function(play) {
	'use strict';
	
	function initialize() {}
	
	function run() {
		play.initialize('credits-screen');
	}
	
	return {
		initialize : initialize,
		run : run
	};
}(FlappyGame.play));